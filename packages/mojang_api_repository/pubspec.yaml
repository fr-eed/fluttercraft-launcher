name: mojang_api_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  json_annotation: ^4.9.0
  url_launcher: ^6.1.8
  http: ^1.2.2
  beaver_log:
    path: ../beaver_log
  path_provider: ^2.1.5
  file_selector: ^1.0.3
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.3
